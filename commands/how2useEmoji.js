/*const { MessageEmbed } = require('discord.js')

module.exports = {
    name: 'emoji',
    permissions: [''],
    execute(client, message, cmd, args, Discord) {
        
        const embed = new MessageEmbed()
            .setColor('#3478ff')
            .setTitle('How to use custom emojis:')
            .setDescription('const [emojiVariable] = client.emojis.cache.get("Emoji ID")')


        message.channel.send(embed)
    }
}*/